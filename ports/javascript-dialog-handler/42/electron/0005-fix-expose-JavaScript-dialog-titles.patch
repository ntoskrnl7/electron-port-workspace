From f3ca312b1d8b6b90bc34fa8e2a74d481a9815eee Mon Sep 17 00:00:00 2001
From: Codex <codex@localhost>
Date: Wed, 20 May 2026 20:52:48 +0900
Subject: [PATCH 5/5] fix: expose JavaScript dialog titles

---
 BUILD.gn                                         | 1 +
 docs/api/structures/alert-javascript-dialog.md   | 1 +
 docs/api/structures/confirm-javascript-dialog.md | 1 +
 docs/api/structures/prompt-javascript-dialog.md  | 1 +
 lib/browser/api/web-contents.ts                  | 5 +++--
 shell/browser/api/electron_api_web_contents.cc   | 7 +++++++
 spec/chromium-spec.ts                            | 5 +++++
 spec/ts-smoke/electron/main.ts                   | 4 ++--
 typings/internal-electron.d.ts                   | 2 +-
 9 files changed, 22 insertions(+), 5 deletions(-)

diff --git a/BUILD.gn b/BUILD.gn
index c50d1641ea..cedc92eef3 100644
--- a/BUILD.gn
+++ b/BUILD.gn
@@ -495,6 +495,7 @@ source_set("electron_lib") {
     "//components/embedder_support:user_agent",
     "//components/heap_profiling/multi_process",
     "//components/input",
+    "//components/javascript_dialogs/core:util",
     "//components/language/core/browser",
     "//components/memory_system",
     "//components/net_log",
diff --git a/docs/api/structures/alert-javascript-dialog.md b/docs/api/structures/alert-javascript-dialog.md
index 3b6e34e5f6..30112f4b07 100644
--- a/docs/api/structures/alert-javascript-dialog.md
+++ b/docs/api/structures/alert-javascript-dialog.md
@@ -3,6 +3,7 @@
 * `type` string - Can be `alert`.
 * `frame` [WebFrameMain](../web-frame-main.md) - The frame that requested the
   JavaScript dialog.
+* `title` string - The localized Chromium dialog title.
 * `messageText` string - The dialog message text.
 * `url` string - The last committed URL of the frame that requested the dialog.
 * `isMainFrame` boolean - Whether the frame that requested the dialog is the
diff --git a/docs/api/structures/confirm-javascript-dialog.md b/docs/api/structures/confirm-javascript-dialog.md
index 44e36fc4ae..78de03b6f0 100644
--- a/docs/api/structures/confirm-javascript-dialog.md
+++ b/docs/api/structures/confirm-javascript-dialog.md
@@ -3,6 +3,7 @@
 * `type` string - Can be `confirm`.
 * `frame` [WebFrameMain](../web-frame-main.md) - The frame that requested the
   JavaScript dialog.
+* `title` string - The localized Chromium dialog title.
 * `messageText` string - The dialog message text.
 * `url` string - The last committed URL of the frame that requested the dialog.
 * `isMainFrame` boolean - Whether the frame that requested the dialog is the
diff --git a/docs/api/structures/prompt-javascript-dialog.md b/docs/api/structures/prompt-javascript-dialog.md
index d4945143b8..6aa0f4d5bc 100644
--- a/docs/api/structures/prompt-javascript-dialog.md
+++ b/docs/api/structures/prompt-javascript-dialog.md
@@ -3,6 +3,7 @@
 * `type` string - Can be `prompt`.
 * `frame` [WebFrameMain](../web-frame-main.md) - The frame that requested the
   JavaScript dialog.
+* `title` string - The localized Chromium dialog title.
 * `messageText` string - The dialog message text.
 * `defaultPromptText` string - The default text for the prompt dialog.
 * `url` string - The last committed URL of the frame that requested the dialog.
diff --git a/lib/browser/api/web-contents.ts b/lib/browser/api/web-contents.ts
index 9affeac42f..ed0d97a298 100644
--- a/lib/browser/api/web-contents.ts
+++ b/lib/browser/api/web-contents.ts
@@ -361,7 +361,7 @@ type JavaScriptDialogType = 'alert' | 'confirm' | 'prompt' | 'beforeunload';
 interface JavaScriptDialogInfo {
   frame: Electron.WebFrameMain;
   dialogType: JavaScriptDialogType;
-  title?: string;
+  title: string;
   messageText: string;
   defaultPromptText: string;
   isReload?: boolean;
@@ -393,6 +393,7 @@ const createJavaScriptDialog = (
   let handled = false;
   const details = {
     frame: info.frame,
+    title: info.title,
     messageText: info.messageText,
     url: info.frame.url,
     isMainFrame: info.frame.parent === null
@@ -448,7 +449,6 @@ const createJavaScriptDialog = (
         response: {
           ...details,
           type: 'beforeunload',
-          title: info.title ?? '',
           isReload: !!info.isReload,
           leave: () => respond(true),
           stay: () => respond(false)
@@ -1129,6 +1129,7 @@ WebContents.prototype._init = function () {
     const parent = this.getOwnerBrowserWindow();
     const abortController = new AbortController();
     const options: MessageBoxOptions = {
+      title: info.title,
       message: info.messageText,
       checkboxLabel: checkbox,
       signal: abortController.signal,
diff --git a/shell/browser/api/electron_api_web_contents.cc b/shell/browser/api/electron_api_web_contents.cc
index 781432f977..d7b43df6f4 100644
--- a/shell/browser/api/electron_api_web_contents.cc
+++ b/shell/browser/api/electron_api_web_contents.cc
@@ -49,6 +49,7 @@
 #include "chrome/common/pref_names.h"
 #include "components/input/native_web_keyboard_event.h"
 #include "components/input/render_widget_host_input_event_router.h"
+#include "components/javascript_dialogs/core/dialog_util.h"
 #include "components/prefs/pref_service.h"
 #include "components/prefs/scoped_user_pref_update.h"
 #include "components/security_state/content/content_utils.h"
@@ -7722,11 +7723,17 @@ void WebContents::RunJavaScriptDialog(content::WebContents* web_contents,
                                       bool* did_suppress_message) {
   CHECK_EQ(web_contents, this->web_contents());
 
+  const std::u16string title = javascript_dialogs::util::DialogTitle(
+      web_contents->GetPrimaryMainFrame()->GetLastCommittedURL(),
+      web_contents->GetPrimaryMainFrame()->GetLastCommittedOrigin(),
+      rfh->GetLastCommittedOrigin());
+
   auto* isolate = JavascriptEnvironment::GetIsolate();
   v8::HandleScope scope(isolate);
   auto info = gin::DataObjectBuilder(isolate)
                   .Set("frame", rfh)
                   .Set("dialogType", dialog_type)
+                  .Set("title", title)
                   .Set("messageText", message_text)
                   .Set("defaultPromptText", default_prompt_text)
                   .Build();
diff --git a/spec/chromium-spec.ts b/spec/chromium-spec.ts
index bf9851d0b2..fba3052d57 100644
--- a/spec/chromium-spec.ts
+++ b/spec/chromium-spec.ts
@@ -3476,6 +3476,7 @@ describe('chromium features', () => {
         w.webContents.executeJavaScript('alert("hello")', true);
         const [info] = await p;
         expect(info.frame).to.equal(w.webContents.mainFrame);
+        expect(info.title).to.not.equal('');
         expect(info.messageText).to.equal('hello');
         expect(info.dialogType).to.equal('alert');
       });
@@ -3504,6 +3505,7 @@ describe('chromium features', () => {
         const resultPromise = w.webContents.executeJavaScript('confirm("hello")', true);
         const [info, cb] = await p;
         expect(info.frame).to.equal(w.webContents.mainFrame);
+        expect(info.title).to.not.equal('');
         expect(info.messageText).to.equal('hello');
         expect(info.dialogType).to.equal('confirm');
         cb(true, '');
@@ -3527,6 +3529,7 @@ describe('chromium features', () => {
         const resultPromise = w.webContents.executeJavaScript('prompt("hello", "default text")', true);
         const [info, cb] = await p;
         expect(info.frame).to.equal(w.webContents.mainFrame);
+        expect(info.title).to.not.equal('');
         expect(info.messageText).to.equal('hello');
         expect(info.defaultPromptText).to.equal('default text');
         expect(info.dialogType).to.equal('prompt');
@@ -3546,6 +3549,7 @@ describe('chromium features', () => {
             return Promise.resolve({ response: 0, checkboxChecked: false });
           };
           const result = await w.webContents.executeJavaScript('prompt("hello", "default text")', true);
+          expect(recordedOpts?.title).to.not.equal('');
           expect(recordedOpts?.message).to.equal('hello');
           expect(recordedOpts?.buttons).to.deep.equal(['OK', 'Cancel']);
           expect(result).to.equal('default text');
@@ -3566,6 +3570,7 @@ describe('chromium features', () => {
           expect(dialog.frame).to.equal(w.webContents.mainFrame);
           expect(dialog.url).to.equal('about:blank');
           expect(dialog.isMainFrame).to.equal(true);
+          expect(dialog.title).to.not.equal('');
 
           switch (dialog.type) {
             case 'alert':
diff --git a/spec/ts-smoke/electron/main.ts b/spec/ts-smoke/electron/main.ts
index c4be4af3a2..c5c32a4349 100644
--- a/spec/ts-smoke/electron/main.ts
+++ b/spec/ts-smoke/electron/main.ts
@@ -132,7 +132,7 @@ app.whenReady().then(() => {
   });
   mainWindow.webContents.setPrintRequestHandler(null);
   mainWindow.webContents.setJavaScriptDialogHandler((dialog) => {
-    console.log(dialog.type, dialog.messageText);
+    console.log(dialog.type, dialog.title, dialog.messageText);
     console.log(dialog.frame, dialog.url, dialog.isMainFrame);
     switch (dialog.type) {
       case 'alert':
@@ -149,7 +149,7 @@ app.whenReady().then(() => {
         console.log(dialog.isReload);
         break;
       case 'beforeunload':
-        console.log(dialog.title, dialog.isReload);
+        console.log(dialog.isReload);
         dialog.leave();
         break;
     }
diff --git a/typings/internal-electron.d.ts b/typings/internal-electron.d.ts
index 73053591f6..9436d6afb7 100644
--- a/typings/internal-electron.d.ts
+++ b/typings/internal-electron.d.ts
@@ -327,7 +327,7 @@ declare namespace Electron {
         info: {
           frame: WebFrameMain;
           dialogType: 'prompt' | 'confirm' | 'alert' | 'beforeunload';
-          title?: string;
+          title: string;
           messageText: string;
           defaultPromptText: string;
           isReload?: boolean;
-- 
2.53.0

