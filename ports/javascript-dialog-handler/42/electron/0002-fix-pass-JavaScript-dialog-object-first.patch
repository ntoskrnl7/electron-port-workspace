From 7661c0bfd58eabba9172547e3861a10b39efa095 Mon Sep 17 00:00:00 2001
From: Codex <codex@localhost>
Date: Wed, 20 May 2026 14:10:30 +0900
Subject: [PATCH 2/5] fix: pass JavaScript dialog object first

---
 docs/api/web-contents.md        | 4 ++--
 lib/browser/api/web-contents.ts | 4 ++--
 spec/api-web-contents-spec.ts   | 4 ++--
 spec/chromium-spec.ts           | 4 ++--
 spec/ts-smoke/electron/main.ts  | 2 +-
 5 files changed, 9 insertions(+), 9 deletions(-)

diff --git a/docs/api/web-contents.md b/docs/api/web-contents.md
index de9f455139..ecc495757b 100644
--- a/docs/api/web-contents.md
+++ b/docs/api/web-contents.md
@@ -1636,8 +1636,8 @@ mainWindow.webContents.setWindowOpenHandler((details) => {
 #### `contents.setJavaScriptDialogHandler(handler)`
 
 * `handler` Function | null
-  * `details` [JavaScriptDialogHandlerDetails](structures/javascript-dialog-handler-details.md)
   * `dialog` [AlertJavaScriptDialog](structures/alert-javascript-dialog.md) | [ConfirmJavaScriptDialog](structures/confirm-javascript-dialog.md) | [PromptJavaScriptDialog](structures/prompt-javascript-dialog.md) | [BeforeUnloadJavaScriptDialog](structures/before-unload-javascript-dialog.md)
+  * `details` [JavaScriptDialogHandlerDetails](structures/javascript-dialog-handler-details.md)
 
 Sets a handler for renderer-initiated JavaScript dialogs. Passing `null` clears
 the handler and restores Electron's default handling.
@@ -1657,7 +1657,7 @@ const { BrowserWindow } = require('electron')
 
 const win = new BrowserWindow()
 
-win.webContents.setJavaScriptDialogHandler(async (details, dialog) => {
+win.webContents.setJavaScriptDialogHandler(async (dialog, details) => {
   switch (dialog.type) {
     case 'alert':
       dialog.ok()
diff --git a/lib/browser/api/web-contents.ts b/lib/browser/api/web-contents.ts
index 7e20aa3e26..e390d27a0c 100644
--- a/lib/browser/api/web-contents.ts
+++ b/lib/browser/api/web-contents.ts
@@ -372,7 +372,7 @@ type JavaScriptDialog =
   Electron.ConfirmJavaScriptDialog |
   Electron.PromptJavaScriptDialog |
   Electron.BeforeUnloadJavaScriptDialog;
-type JavaScriptDialogHandler = (details: Electron.JavaScriptDialogHandlerDetails, dialog: JavaScriptDialog) => void | Promise<void>;
+type JavaScriptDialogHandler = (dialog: JavaScriptDialog, details: Electron.JavaScriptDialogHandlerDetails) => void | Promise<void>;
 type PendingJavaScriptDialog = { cancel: () => void };
 type JavaScriptDialogController = PendingJavaScriptDialog & { response: JavaScriptDialog };
 
@@ -496,7 +496,7 @@ const runJavaScriptDialogHandler = (
   pending.add(dialog);
 
   try {
-    Promise.resolve(handler(createJavaScriptDialogDetails(info), dialog.response)).catch(error => {
+    Promise.resolve(handler(dialog.response, createJavaScriptDialogDetails(info))).catch(error => {
       cancelJavaScriptDialog(dialog.response);
       process.nextTick(() => {
         throw error;
diff --git a/spec/api-web-contents-spec.ts b/spec/api-web-contents-spec.ts
index 0dc16dfcd9..ee679e5c72 100644
--- a/spec/api-web-contents-spec.ts
+++ b/spec/api-web-contents-spec.ts
@@ -210,7 +210,7 @@ describe('webContents module', () => {
       w.webContents.on('will-prevent-unload', () => {
         legacyEventFired = true;
       });
-      w.webContents.setJavaScriptDialogHandler((details, dialog) => {
+      w.webContents.setJavaScriptDialogHandler((dialog, details) => {
         expect(details.type).to.equal('beforeunload');
         expect(details.frame).to.equal(w.webContents.mainFrame);
         expect(details.isMainFrame).to.equal(true);
@@ -240,7 +240,7 @@ describe('webContents module', () => {
       w.webContents.on('will-prevent-unload', () => {
         legacyEventFired = true;
       });
-      w.webContents.setJavaScriptDialogHandler((details, dialog) => {
+      w.webContents.setJavaScriptDialogHandler((dialog, details) => {
         expect(details.type).to.equal('beforeunload');
         if (dialog.type === 'beforeunload') {
           setImmediate(() => {
diff --git a/spec/chromium-spec.ts b/spec/chromium-spec.ts
index 2875bcd328..39abdd3624 100644
--- a/spec/chromium-spec.ts
+++ b/spec/chromium-spec.ts
@@ -3561,7 +3561,7 @@ describe('chromium features', () => {
         w.loadURL('about:blank');
 
         const seen: string[] = [];
-        w.webContents.setJavaScriptDialogHandler((details, dialog) => {
+        w.webContents.setJavaScriptDialogHandler((dialog, details) => {
           seen.push(`${details.type}:${details.messageText}`);
           expect(details.frame).to.equal(w.webContents.mainFrame);
           expect(details.url).to.equal('about:blank');
@@ -3603,7 +3603,7 @@ describe('chromium features', () => {
         w.loadURL('about:blank');
 
         let called = false;
-        w.webContents.setJavaScriptDialogHandler((details, dialog) => {
+        w.webContents.setJavaScriptDialogHandler((dialog, details) => {
           called = true;
           if (dialog.type === 'alert') dialog.ok();
         });
diff --git a/spec/ts-smoke/electron/main.ts b/spec/ts-smoke/electron/main.ts
index 2a92488cde..862a77a69d 100644
--- a/spec/ts-smoke/electron/main.ts
+++ b/spec/ts-smoke/electron/main.ts
@@ -131,7 +131,7 @@ app.whenReady().then(() => {
     });
   });
   mainWindow.webContents.setPrintRequestHandler(null);
-  mainWindow.webContents.setJavaScriptDialogHandler((details, dialog) => {
+  mainWindow.webContents.setJavaScriptDialogHandler((dialog, details) => {
     console.log(details.type, details.messageText);
     switch (dialog.type) {
       case 'alert':
-- 
2.53.0

